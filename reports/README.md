# Research reports

The pipeline generates `report.md` inside each configured output directory, with
linked figures and numerical tables. Synthetic reports are explicitly labeled.
No real IXI result is included in this repository until an empirical experiment
has been run and reviewed. Generated data and reports stay outside Git by default.
